1. Error codes

2. exit

3. Specific cases:
	* echo -n safas > file	-> Doesnt redir to file

4. c-d with "<< "

5. Bash exit code:

	* case:
		minishell: ^C
		minishell: ^D (exit)

		zsh% echo $?
		130

		(128 + ^C code)
